---
import Layout from "../layouts/Layout.astro";
---

<Layout title="SUKAIRAIDA">
  <main>
    <section class="hero">
      <div class="overlay"></div>
      <i
        ><svg
          xmlns="http://www.w3.org/2000/svg"
          width="40"
          height="40"
          fill="white"
          class="bi bi-chevron-down"
          viewBox="0 0 16 16"
        >
          <path
            fill-rule="evenodd"
            d="M1.646 4.646a.5.5 0 0 1 .708 0L8 10.293l5.646-5.647a.5.5 0 0 1 .708.708l-6 6a.5.5 0 0 1-.708 0l-6-6a.5.5 0 0 1 0-.708z"
          ></path>
        </svg></i
      >
    </section>
    <section class="main-content">content</section>
  </main>
</Layout>

<style>
  .hero {
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background-image: url("/hero-bg.jpeg");
    background-position: center;
    position: relative;
  }

  .hero i {
    z-index: 2;
    margin-top: auto;
    animation: caret infinite 1s alternate;
    margin-bottom: 2rem;
  }

  .main-content {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.4);
  }

  @keyframes caret {
    0% {
      opacity: 0.5;
      transform: translateY(0px);
    }

    100% {
      opacity: 1;
      transform: translateY(1rem);
    }
  }
</style>
